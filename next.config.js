/** @type {import('next').NextConfig} */
const nextConfig = {
  // Environment variables are automatically loaded from .env.local
  // No need to explicitly define them here for Next.js 14
}

module.exports = nextConfig